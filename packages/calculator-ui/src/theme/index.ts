/**
 * Theme configuration exports
 */

import { createTheme } from '@mui/material/styles';

export const pricingCalculatorTheme = createTheme({
  palette: {
    primary: {
      main: '#1976d2',
    },
    secondary: {
      main: '#dc004e',
    },
  },
  typography: {
    fontFamily: '"Roboto", "Helvetica", "Arial", sans-serif',
  },
});

